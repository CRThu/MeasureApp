<UserControl x:Class="MeasureApp.View.PluginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MeasureApp.View"
             xmlns:vm="clr-namespace:MeasureApp.ViewModel"
             xmlns:v="clr-namespace:MeasureApp.View"
             xmlns:c="clr-namespace:MeasureApp.View.Converter"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:ikriv="clr-namespace:IKriv.Windows.Controls.Behaviors"
             d:DataContext="{Binding Source={StaticResource Locator},Path=PluginUI}"
             DataContext="{Binding Source={StaticResource Locator},Path=PluginUI}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
    </UserControl.Resources>
    <hc:SimpleStackPanel Orientation="Vertical">
        <hc:SimpleStackPanel Orientation="Vertical">
            <Button Content="OpenPlugin" Command="{Binding OpenCommand}" HorizontalAlignment="Left"/>
        </hc:SimpleStackPanel>
        <hc:TabControl ItemsSource="{Binding Source={StaticResource Locator},Path=PluginSrv.Plugins}" SelectedItem="{Binding SelectedPlugin}" ikriv:TabContent.IsCached="True">
            <hc:TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}"/>
                </DataTemplate>
            </hc:TabControl.ItemTemplate>
            <ikriv:TabContent.Template>
                <DataTemplate>
                    <ContentControl Content="{Binding View}"/>
                </DataTemplate>
            </ikriv:TabContent.Template>
        </hc:TabControl>
    </hc:SimpleStackPanel>
</UserControl>
