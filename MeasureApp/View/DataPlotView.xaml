<UserControl x:Class="MeasureApp.View.DataPlotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MeasureApp.View"
             xmlns:vm="clr-namespace:MeasureApp.ViewModel"
             xmlns:v="clr-namespace:MeasureApp.View"
             xmlns:c="clr-namespace:MeasureApp.View.Converter"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             xmlns:b="clr-namespace:MeasureApp.View.Behaviors"
             d:DataContext="{Binding Source={StaticResource Locator},Path=DataPlotVM}"
             DataContext="{Binding Source={StaticResource Locator},Path=DataPlotVM}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="Data Y: " VerticalAlignment="Center" Margin="5,0"/>
            <hc:ComboBox Grid.Row="0" Grid.Column="2" ItemsSource="{Binding Source={StaticResource Locator},Path=DataLogger.Keys}" SelectedItem="{Binding SelectedDataYKey}" Width="200" Margin="5,2"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="Data X: " VerticalAlignment="Center" Margin="5,0"/>
            <CheckBox Grid.Row="1" Grid.Column="0" IsChecked="{Binding IsCustomDataXUsed}" VerticalAlignment="Center" Margin="5,0"/>
            <hc:ComboBox Grid.Row="1" Grid.Column="2" ItemsSource="{Binding Source={StaticResource Locator},Path=DataLogger.Keys}" SelectedItem="{Binding SelectedDataXKey}" Width="200" Margin="5,2" IsEnabled="{Binding IsCustomDataXUsed}"/>
        </Grid>
        <hc:SimpleStackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
            <CheckBox Content="自动刷新" Margin="5,0" IsChecked="{Binding IsAutoUpdateEnabled}"/>
            <Button Content="手动刷新" Margin="5,0" Style="{StaticResource ButtonDefault.Small}" Command="{Binding ManualRefreshCommand}"/>
            <Button Content="测试生成" Margin="5,0" Style="{StaticResource ButtonDefault.Small}" Command="{Binding GenTestDataCommand}"/>
            <Button Content="测试新增" Margin="5,0" Style="{StaticResource ButtonDefault.Small}" Command="{Binding AddTestDataCommand}"/>
        </hc:SimpleStackPanel>
        <ScottPlot:WpfPlot Grid.Row="2">
            <i:Interaction.Behaviors>
                <b:ScottPlotBehavior Plot="{Binding Plot, Mode=OneWayToSource}" IsAutoUpdateEnabled="{Binding IsAutoUpdateEnabled}"/>
            </i:Interaction.Behaviors>
        </ScottPlot:WpfPlot>
    </Grid>
</UserControl>
