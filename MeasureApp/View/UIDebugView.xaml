<UserControl x:Class="MeasureApp.View.UIDebugView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MeasureApp.View"
             xmlns:vm="clr-namespace:MeasureApp.ViewModel"
             xmlns:v="clr-namespace:MeasureApp.View"
             xmlns:c="clr-namespace:MeasureApp.View.Converter"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             DataContext="{Binding Source={StaticResource Locator},Path=UIDebug}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
    </UserControl.Resources>
    <hc:SimpleStackPanel Orientation="Horizontal">
        <ListView ItemsSource="{Binding Source={StaticResource Locator},Path=Layout.Anchorables}" SelectedItem="{Binding SelectedTool}" DisplayMemberPath="Title" Style="{StaticResource BaseStyle}" Width="200" Height="300" VerticalAlignment="Top"/>
        <Button Content="UIUpdateDocument" Command="{Binding UpdateDocumentCommand}" VerticalAlignment="Top"/>
        <Button Content="UIUpdateAnchorable" Command="{Binding UpdateAnchorableCommand}" VerticalAlignment="Top"/>
    </hc:SimpleStackPanel>
</UserControl>
