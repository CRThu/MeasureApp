<UserControl x:Class="MeasureApp.View.DataProcView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MeasureApp.View"
             xmlns:vm="clr-namespace:MeasureApp.ViewModel"
             xmlns:v="clr-namespace:MeasureApp.View"
             xmlns:c="clr-namespace:MeasureApp.View.Converter"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             DataContext="{Binding Source={StaticResource Locator},Path=DataProc}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="5,5,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="INL.SRC.X" Margin="5,0,5,0" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1" ItemsSource="{Binding Context.DataLogger.Keys}" Width="150" Margin="5,0,5,0"/>
            <TextBlock Grid.Column="2" Text="INL.SRC.Y" Margin="5,0,5,0" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="3" ItemsSource="{Binding Context.DataLogger.Keys}" Width="150" Margin="5,0,5,0"/>
            <TextBlock Grid.Column="4" Text="INL.DST.Y" Margin="5,0,5,0" VerticalAlignment="Center"/>
            <TextBox Grid.Column="5" Width="150" Margin="5,0,5,0"/>
            <Button Grid.Column="6" Content="运算" Width="60" Margin="5,0,5,0"/>
        </Grid>
    </Grid>
</UserControl>
